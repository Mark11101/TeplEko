import { StyleSheet } from "react-native";

import { borderRadius } from "../../../styles/variables.styles";

const styles = StyleSheet.create({
	panel: {
		borderRadius,
		backgroundColor: 'white',
	}
});

export default styles
